#include <bits/stdc++.h>
using namespace std;

int main()
{
    size_t t;
    cin>>t;

    while(t--)
    {
        string str;
        cin>>str;
        size_t n=str.length();
        if((str[0]>122 || str[0]<97) || (str[n-1]>122 || str[n-1]<97))
            cout<<"NO"<<endl;
        else
        {
            for(size_t i=0;i<n;i++)
            {

            }
        }
    }

    return 0;
}
